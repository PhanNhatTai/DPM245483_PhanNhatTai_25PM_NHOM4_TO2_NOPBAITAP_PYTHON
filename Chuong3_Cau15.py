"""
(a) range(5) kết quả vòng lặp chạy từ 0 đến 4
(b) range(5, 10) kết quả chạy từ 5 đến 9
(c) range(5, 20, 3)  kết quả chạy từ 5 đến 17 mỗi lần chạy 3 đơn vị
(d) range(20, 5, -1) kết quả chạy lui từ 20 về 6 mỗi lần giảm 1 đơn vị
(e) range(20, 5, -3) kết quả chạy lui từ 20 về 8 mỗi lần giảm 3 đơn vị
(f) range(10, 5) vòng lặp sẽ không chạy 
(g) range(0) kết quả vòng lặp chạy từ 0 đến 0
(h) range(10, 101, 10) kết quả vòng lặp chạy từ 10 đến 100 mỗi lần chạy 10 đơn vị
(i) range(10, -1, -1) kết quả chạy lui từ 10 về 0 mỗi lần giảm 1 đơn vị
(j) range(-3, 4) kết quả chạy từ -3 đến 3 mỗi lần chạy 1 đơn vị
(k) range(0, 10, 1) kết quả chạy từ 0 đến 9 mỗi lần chạy 1 đơn vị
"""